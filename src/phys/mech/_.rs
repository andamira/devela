// devela/src/phys/mech/_.rs
//
#![doc = crate::_tags!(wip)]
#![doc = crate::_DOC_PHYS_MECH!()] // public
#![doc = crate::_doc!(modules: crate::phys; mech)]
#![doc = crate::_doc!(flat:"phys")]
#![doc = crate::_doc!(hr)]
//

crate::mods_in! {
    // pub mod_ deform; // Deformation measures, rest states, and mechanical response laws
    // pub mod_ fluid; // Pressure, flow, and fluid-mechanical systems
    // pub mod_ inter;  // Mechanical interactions, constraints, contacts, and joints
    // pub mod_ kine;   // Motion state and kinematic relations through time
    // pub mod_ load;   // Applied mechanical actions such as force, torque, and impulse
    // pub mod_ rigid; // Rigid-body mechanical models
    // pub mod_ soft;  // Deformable mechanical models and reduced forms
    // pub mod_ trans;  // Mechanical transmission, transformation, and transduction
}
crate::mods_out! { // _pub_mods
    _pub_mods {
        // pub use super::{
        //     deform::_all::*,
        //     fluid::_all::*,
        //     inter::_all::*,
        //     kine::_all::*,
        //     load::_all::*,
        //     rigid::_all::*,
        //     soft::_all::*,
        //     trans::_all::*,
        // };
    }
}
