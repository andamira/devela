// devela/src/phys/mech/fluid/mod.rs
//
#![doc = crate::_tags!(wip)]
#![doc = crate::_DOC_PHYS_MECH_FLUID!()] // WIP
#![doc = crate::_doc!(modules: crate::phys::mech; fluid)]
#![doc = crate::_doc!(flat:"phys")]
#![doc = crate::_doc!(hr)]
//

// mod chamber; // Pressure-volume behavior of enclosed and compliant fluid domains
// mod flow; // Fluid motion, transport, resistance, and momentum transfer
// mod network; // Lumped fluid networks of channels, valves, and connected elements
// mod spatial; // Spatially distributed fluid models

crate::mods_out! { // _mods
    _mods {
        // pub use super::{
        //     chamber::_all::*,
        //     flow::_all::*,
        //     network::_all::*,
        //     spatial::_all::*,
        // };
    }
}
