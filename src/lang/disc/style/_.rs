// devela/src/lang/disc/style/_.rs
//
//! Reusable dimensions and profiles of stylistic expression.
//

crate::mods_in! {
    // mod_ cadence; // Rhythmic movement and pacing of expression
    // mod_ diction; // Characteristic selection and arrangement of words
    // mod_ marker; // Detectable features associated with a style
    // mod_ profile; // Composed descriptions of multiple stylistic dimensions
    // mod_ register; // Variation according to setting, audience, and social role
    // mod_ tone; // Expressive attitude toward subject, audience, or situation
}
crate::mods_out! { // _mods
    _mods {
        // pub use super::{
        //     cadence::_all::*,
        //     diction::_all::*,
        //     marker::_all::*,
        //     profile::_all::*,
        //     register::_all::*,
        //     tone::_all::*,
        // };
    }
}
