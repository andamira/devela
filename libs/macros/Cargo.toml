## Table of Contents
#
# * [package]
# * [lib]
#
# * [features]
#   - Environment       (std,alloc,no_std)
#   - Safety            (safe,unsafe,safest)
#   - Nightly
#   - Capability
#     - default
#     - docs
#   - Dependency
#
# * [dependencies]
#
# - [package.metadata.*]
# - [badges]
#
# - lints
#   - [lints.rust]
#   - [lints.rustdoc]
#   - [lints.clippy]
#   - [lints.rust.unexpected_cfgs]
#
# - [[example]]

[package]
name = "devela_macros"
description = "procedural macros for devela"
version = "0.13.0-wip"
rust-version = "1.89.0" # sync with .github/workflows/check.yml, check.sh, README.md
edition = "2021"
resolver = "3"
authors = ["José Luis Cruz <joseluis@andamira.net>"]
repository = "https://github.com/andamira/devela/tree/main/libs/macros"
documentation = "https://docs.rs/devela_macros"
license = "MIT OR Apache-2.0"
include = [
	"/Cargo.toml",
	"/LICENSE*",
	"/src/**/*.rs", "/src/**/*.md",
	# examples used in docs:
	"/examples/*.rs",
]
categories = ["rust-patterns", "development-tools::procedural-macro-helpers"]
keywords = ["dev", "helper", "helpers", "extension", "code-generation"]
autopublish = false
publish = true

[lib]
path = "./src/lib.rs"
proc-macro = true


[features] # 9/300 (291 remaining), 8 visible, 1 hidden
# ==============================================================================

#* Environment features *#
# ------------------------------------------------------------------------------
alloc = []
std = ["alloc"]


#* Safety features *#
# ------------------------------------------------------------------------------
unsafe = [] # enables unsafe features in this crate (unused)
safe = [] # forbids `unsafe` in this crate


#* Nightly features *#
# ------------------------------------------------------------------------------
nightly = ["nightly_doc"]
	nightly_doc = [] # doc_cfg


#* Capability features *#
# ------------------------------------------------------------------------------
default = ["std", "safe"]

#* docs capabilities *#
_docsrs = ["nightly", "std", "unsafe", "dep_hashbrown"] # complete documentation


#* Dependency features *#
# ------------------------------------------------------------------------------
dep_hashbrown = ["dep:hashbrown", "alloc"]


[dependencies] # 3 (2 required, 1 optional)
# ==============================================================================
# WAIT: [proc_macro_quote](https://github.com/rust-lang/rust/issues/54722)
proc-macro2 = "1.0.95"
quote = "1.0.40"

[dependencies.hashbrown]
version = "0.15.1"
optional = true
default-features = false
features = ["default-hasher", "inline-more"]


[package.metadata.docs.rs]
# ==============================================================================
no-default-features = true
features = ["_docsrs"]


[badges]
# ==============================================================================
maintenance = { status = "experimental" }


# examples
# ==============================================================================
[[example]]
name = "cif"
required-features = ["alloc"]
[[example]]
name = "coalesce"
required-features = ["alloc"]
[[example]]
name = "compile_attr"
required-features = ["alloc"]
[[example]]
name = "compile_doc"
required-features = ["alloc"]
[[example]]
name = "compile"
required-features = ["alloc"]
[[example]]
name = "enumint"
required-features = ["alloc"]
